name: Release Plugin

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4.3.1
        with:
          dotnet-version: "9.0.x"

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Generate changelog from commits since last tag
        id: changelog
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          if [ -n "$PREV_TAG" ]; then
            git log "${PREV_TAG}..HEAD" --pretty=format:"- %s" \
              | grep -v '\[skip ci\]' | head -20 > changelog.txt
          else
            echo "Initial release." > changelog.txt
          fi

      - name: Build (Release)
        run: >
          dotnet publish Jellyfin.Plugin.Template.csproj -c Release -o ./publish --nologo
          /p:Version=${{ steps.version.outputs.VERSION }}
          /p:AssemblyVersion=${{ steps.version.outputs.VERSION }}

      - name: Package zip
        run: |
          mkdir -p dist
          zip -j "dist/Jellyfin.Plugin.Template_${{ steps.version.outputs.VERSION }}.zip" \
              publish/Jellyfin.Plugin.Template.dll

      - name: Calculate MD5 checksum
        id: checksum
        run: |
          MD5=$(md5sum "dist/Jellyfin.Plugin.Template_${{ steps.version.outputs.VERSION }}.zip" \
                | awk '{print $1}' | tr '[:lower:]' '[:upper:]')
          echo "MD5=$MD5" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          ZIP="dist/Jellyfin.Plugin.Template_${VERSION}.zip"
          NOTES=$(cat changelog.txt)
          # Delete any pre-existing release for this tag (handles tag-deletion/recreation).
          gh release delete "v${VERSION}" --repo "${{ github.repository }}" --yes 2>/dev/null || true
          gh release create "v${VERSION}" "$ZIP" \
            --repo "${{ github.repository }}" \
            --title "v${VERSION}" \
            --notes "$NOTES"

      - name: Commit updated manifest.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git checkout main
          git pull --ff-only origin main
          VERSION="${{ steps.version.outputs.VERSION }}"
          CHECKSUM="${{ steps.checksum.outputs.MD5 }}"
          ZIP="Jellyfin.Plugin.Template_${VERSION}.zip"
          URL="https://github.com/${{ github.repository }}/releases/download/v${VERSION}/${ZIP}"
          TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          # Bump AssemblyVersion in csproj so local builds match the release.
          sed -i "s|<AssemblyVersion>.*</AssemblyVersion>|<AssemblyVersion>${VERSION}.0</AssemblyVersion>|" Jellyfin.Plugin.Template.csproj
          sed -i "s|<Version>.*</Version>|<Version>${VERSION}.0</Version>|" Jellyfin.Plugin.Template.csproj

          # Prepend new version entry; remove any existing entry with the same version (idempotent).
          # Version uses 4 parts (e.g. 0.5.4.0) to match the DLL AssemblyVersion exactly.
          jq \
            --arg ver  "${VERSION}.0" \
            --arg url  "$URL" \
            --arg sum  "$CHECKSUM" \
            --arg ts   "$TIMESTAMP" \
            --rawfile  cl changelog.txt \
            '.[0].versions = [
               {
                 "version":   $ver,
                 "changelog": ($cl | rtrimstr("\n")),
                 "targetAbi": "10.9.0.0",
                 "sourceUrl": $url,
                 "checksum":  $sum,
                 "timestamp": $ts
               }
             ] + (.[0].versions | map(select(.version != $ver)))' \
            manifest.json > manifest.tmp.json
          mv manifest.tmp.json manifest.json
          git add manifest.json Jellyfin.Plugin.Template.csproj
          git diff --staged --quiet || \
            git commit -m "chore: update manifest and version for v${VERSION} [skip ci]"
          git push origin main
